#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll n, m;
ll a[2005][2005];

int main() {
    cin >> n >> m;
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < m; j++) {
            
        }
    }
}