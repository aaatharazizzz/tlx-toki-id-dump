#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int N, M;

void printk(const int &a, const int &b)
{
    cout << "? " << a << ' ' << b << '\n' << flush;
}
//min (min(1 <= i <= N | x - Ai|), min(1<=i<=M | y - Bi|))
int main()
{
    int x, y;
    return 0;
}